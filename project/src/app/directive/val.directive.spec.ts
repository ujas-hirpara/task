import { ValDirective } from './val.directive';

describe('ValDirective', () => {
  it('should create an instance', () => {
    const directive = new ValDirective();
    expect(directive).toBeTruthy();
  });
});
